# MCUboot + signed OTA images (app_update.bin) for mcumgr BLE DFU.
SB_CONFIG_BOOTLOADER_MCUBOOT=y

# nRF52832 is not covered by the NCS default ECDSA default (nRF52840-only).
SB_CONFIG_BOOT_SIGNATURE_TYPE_ECDSA_P256=y
